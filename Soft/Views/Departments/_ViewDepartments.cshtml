@using Contoso.Pages.Constants;
@using Contoso.Pages.HtmlHelpers;
@model Department

@{
    ViewData[Data.Title] = nameof(Department);
    ViewData[Data.ItemID] = Model?.ID;
}

@Html.ViewItem(x => x.Name)
@Html.ViewItem(x => x.Budget)
@Html.ViewItem(x => x.StartDate)
@*@Html.ViewItem(x => x.RowVersion)
*@@Html.ViewItem(x => x.Administrator.FullName, x => x.Administrator)