@using Contoso.Pages.Constants;
@model Instructor

@{
    ViewData[Data.Title] = nameof(Instructor);
    ViewData[Data.ItemID] = Model.ID;
    Layout = Layouts.Edit;
}

<partial name="_EditInstructors" model="@Model" />