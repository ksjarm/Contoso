@model Course
@using Contoso.Pages.HtmlHelpers

@{
    var items = (IEnumerable<SelectListItem>)ViewBag.DepartmentID;
}

@Html.EditItem(x => x.Title)
@Html.EditItem(x => x.Credits)
@Html.SelectItem(x => x.DepartmentID, items, x => x.Department)