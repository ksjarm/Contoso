@using Contoso.Pages.Constants;
@model Student

@{
    ViewData[Data.Title] = nameof(Student);
    Layout = Layouts.Edit;
}

<input type="hidden" asp-for="ID" />

<partial name="_EditStudents" model="@Model" />
