@using Contoso.Pages.Constants;
@model Student

@{
	ViewData[Data.Title] = nameof(Student);
	ViewData[Data.ItemID] = Model.ID;
	Layout = Layouts.Details;
}

<partial name="_ViewStudents" model="@Model" />

